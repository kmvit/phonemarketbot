# PhoneMarketBot

Telegram-бот для продажи телефонов и электроники с функционалом корзины и заказов.

## 📋 Описание

Бот позволяет пользователям просматривать каталог товаров, добавлять их в корзину и оформлять заказы. Администратор может загружать прайс-листы, управлять наценками и просматривать заказы.

## ✨ Функционал

### Для пользователей:
- 📱 Просмотр каталога товаров по категориям
- 🛒 Добавление товаров в корзину с указанием количества
- ✏️ Изменение количества товаров в корзине
- ❌ Удаление товаров из корзины
- ✅ Оформление заказов
- 💰 Автоматический расчет цены с учетом персональных скидок

### Для администраторов:
- 📊 Загрузка прайс-листов из Excel файлов
- ⚙️ Настройка наценки на товары
- 📦 Просмотр всех заказов с детальной информацией
- 👤 Просмотр информации о пользователях и ссылки для связи
- 📈 Статистика по товарам и категориям

## 🚀 Установка

### Требования
- Python 3.9+
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))

### Шаги установки

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd phonemarketbot
```

2. **Создайте виртуальное окружение:**
```bash
python3 -m venv venv
source venv/bin/activate  # Для Linux/Mac
# или
venv\Scripts\activate  # Для Windows
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Настройте конфигурацию:**
   - Откройте файл `config.py`
   - Укажите ваш `BOT_TOKEN` (получить у [@BotFather](https://t.me/BotFather))
   - Укажите `ADMIN_IDS` - список Telegram ID администраторов

```python
BOT_TOKEN = "ваш_токен_бота"
ADMIN_IDS = [ваш_telegram_id]  # Узнать свой ID можно у @userinfobot
```

5. **Запустите бота:**
```bash
python main.py
```

## 📁 Структура проекта

```
phonemarketbot/
├── admin/              # Административные функции
│   ├── discount.py    # Управление скидками и наценками
│   ├── markup.py      # Клавиатуры для админки
│   └── price_loader.py # Загрузка прайс-листов
├── bot/               # Основная логика бота
│   ├── handlers/      # Обработчики сообщений
│   │   ├── admin.py   # Обработчики для админа
│   │   └── user.py    # Обработчики для пользователей
│   └── keyboards/     # Клавиатуры бота
│       └── category.py # Клавиатуры категорий
├── db/                # Работа с базой данных
│   ├── models.py      # Модели БД
│   ├── crud.py        # CRUD операции
│   └── utils.py       # Утилиты БД
├── services/          # Сервисные функции
├── data/              # Данные (прайс-листы, примеры)
├── config.py         # Конфигурация
├── main.py           # Точка входа
└── requirements.txt  # Зависимости
```

## 🗄️ База данных

Бот использует SQLite базу данных со следующими таблицами:
- `products` - товары
- `cart` - корзина пользователей
- `orders` - заказы
- `order_items` - позиции заказов
- `settings` - настройки (наценка)

База данных создается автоматически при первом запуске.

## 📖 Использование

### Для пользователей:

1. Запустите бота командой `/start`
2. Выберите "Прайс" для просмотра каталога
3. Выберите категорию и подкатегорию
4. Нажмите на строку товара для добавления в корзину
5. Введите количество товара
6. Перейдите в "Корзина" для управления заказом
7. Используйте кнопки ➖ и ➕ для изменения количества
8. Нажмите "Оформить заказ" для завершения

### Для администраторов:

1. Запустите бота и выберите "Админка"
2. **Загрузка прайса:**
   - Выберите "📊 Загрузить прайс"
   - Отправьте Excel файл с прайс-листом
   - Товары автоматически загрузятся в базу

3. **Настройка наценки:**
   - Выберите "⚙️ Настройка наценки"
   - Введите сумму наценки в рублях (например, 100)
   - Наценка будет применяться к новым загрузкам

4. **Просмотр заказов:**
   - Выберите "📦 Заказы"
   - Просмотрите список всех заказов
   - Каждый заказ содержит информацию о пользователе и позициях

## 📝 Формат прайс-листа

Прайс-лист должен быть в формате Excel (.xlsx или .xls). Бот автоматически определяет формат файла и обрабатывает его.

Поддерживаемые форматы:
- Стандартный формат с колонками: Название, Страна, Цена
- Упрощенный формат с названием товара и ценой в одной строке

## ⚙️ Настройки

Настройки проекта хранятся в файле `.env`. Создайте файл `.env` в корне проекта и укажите следующие переменные:

- `BOT_TOKEN` - токен бота от BotFather
- `ADMIN_IDS` - список ID администраторов (через запятую, например: `123456789,987654321`)
- `DATABASE_PATH` - путь к базе данных (по умолчанию: `phonemarketbot.db`)
- `DEFAULT_MARKUP_AMOUNT` - стандартная наценка в рублях (по умолчанию: `0`)
- `PRICE_UPLOAD_DIR` - директория для загруженных прайс-листов (по умолчанию: `data/samples`)

Пример `.env` файла:
```
BOT_TOKEN=your_bot_token_here
ADMIN_IDS=123456789
DATABASE_PATH=phonemarketbot.db
DEFAULT_MARKUP_AMOUNT=0
PRICE_UPLOAD_DIR=data/samples
```

## 🔧 Зависимости

- `aiogram==3.4.1` - фреймворк для Telegram ботов
- `openpyxl==3.1.2` - работа с Excel файлами
- `pandas==2.2.2` - обработка данных
- `sqlite-utils==3.36` - утилиты для SQLite
- `python-dotenv==1.0.0` - загрузка переменных окружения из .env файла

## 📝 Лицензия

Этот проект создан для личного использования.

## 👤 Автор

Проект разработан для управления продажами телефонов и электроники через Telegram.

## 🐛 Поддержка

При возникновении проблем:
1. Проверьте правильность токена бота
2. Убедитесь, что все зависимости установлены
3. Проверьте права доступа к файлам и директориям
4. Убедитесь, что база данных не заблокирована другим процессом

